<div class="menu">
    <div class="bg-img"></div>
    <nav class="navbar navbar-expand-lg navbar-dark  bg-primary custom-navbar " style="flex: 0 0 70px">
        <div class="container-fluid">
            <a class="navbar-brand" href="http://localhost:3000/user"
                style="font-size: 14px; color: black; font-weight: bold; border: 1px solid black; padding: 5px;">CSDL
                TIỀN
                GIANG</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" href="http://localhost:3000/account">Quản lý tài khoản</a>
                    </li>
                </ul>
            </div>
            <div class="d-flex flex-row-reverse bd-highlight" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-dark" href="#" id="navbarDropdown" role="button"
                            data-bs-toggle="dropdown" aria-expanded="false">
                            <% if (user && user.fullname) { %>
                                <%= user.fullname %>
                                    <% } else { %>
                                        Đăng nhập
                                        <% } %>
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <% if (user && user.fullname) { %>
                                <li>
                                    <div class="p-2 bd-highlight">
                                        <form method="POST" action="/logout">
                                            <button type="submit" class="btn btn-primary">Đăng xuất</button>
                                        </form>
                                    </div>
                                </li>
                                <% } else { %>
                                    <li><a class="dropdown-item" href="/login">Đăng nhập</a></li>
                                    <li><a class="dropdown-item" href="/register">Đăng ký</a></li>
                                    <% } %>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</div>